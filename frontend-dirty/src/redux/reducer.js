export default function reducer(state, action) {
    if (state === undefined) {
        return {};
    }

    switch (action.type) {
        // TODO: here is something to work with Redux
        // case "increase":
        //     return { count: count + 1 };
        // case "decrease":
        //     return { count: count - 1 };
        // default:
        //     return state;
    }
}